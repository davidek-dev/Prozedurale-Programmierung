#include <stdio.h>

int main() {
  int a, b; // intialisierung der variablen a&b

  printf("\n\n Hallo TUTOR! \n\n");//ausgabe in der kommandozeile mit zwei zeilenumbrücken am ende
  a = 22; //dekleration von a
  b = 20; //dekleration von b
  printf("Die Antwort ist: \t %d \n\n", a + b); //ausgabe mit %d. %d steht für einen integer wert, welcher aus a+b berechnet wird

  return 0;//function main gibt true an das system zurück
}
